{
  "name": "rn-payments",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ benjlevesque/rn-payments",
    "create-local": "graph create --node http://localhost:8020/ benjlevesque/rn-payments",
    "remove-local": "graph remove --node http://localhost:8020/ benjlevesque/rn-payments",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 benjlevesque/rn-payments"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.20.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
